---
 - hosts: webservers
   become: yes
   tasks:
   - name: install httpd
     yum:
       name: httpd
       state: latest
   - name: copy index.html
     copy:
       src: index.html
       dest: /var/www/html
   - name: start service
     service:
          name: httpd
          state: started
